
export const API_PREFIX = '/api';

export const AUTH_ENDPOINTS = {
  SIGNUP: `${API_PREFIX}/signup`,
  LOGIN: `${API_PREFIX}/login`,
  LOGOUT: `${API_PREFIX}/logout`,
  REFRESH_TOKEN: `${API_PREFIX}/refreshAccessToken`,
};
