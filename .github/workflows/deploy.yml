name: Deploy to Production

on:
  push:
    branches: [main]

# NODE_VERSION은 .nvmrc 파일에서 자동으로 읽어옴

jobs:
  test:
    name: 배포 전 테스트
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
          MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
        ports:
          - 3307:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: "backend/.nvmrc"
          cache: "npm"
          cache-dependency-path: "backend/package-lock.json"

      - run: npm ci
      - run: npx prisma generate
      - run: npx prisma migrate deploy
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
      - run: npm run lint
      - run: npm run build

  # Docker 이미지 빌드 및 푸시
  # build-and-push:
  #   name: Docker 이미지 빌드 및 푸시
  #   runs-on: ubuntu-latest
  #   needs: test

  #   steps:
  #     - name: 코드 체크아웃
  #       uses: actions/checkout@v4

  #     - name: Docker 메타데이터 생성
  #       id: meta
  #       uses: docker/metadata-action@v5
  #       with:
  #         images: sansan12/mysql
  #         tags: |
  #           type=ref,event=branch
  #           type=sha,prefix={{branch}}-
  #           type=raw,value=latest,enable={{is_default_branch}}

  #     - name: Docker Buildx 설정
  #       uses: docker/setup-buildx-action@v3

  #     - name: Docker Hub 로그인
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}

  #     - name: Docker 이미지 빌드 및 푸시
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: true
  #         tags: ${{ steps.meta.outputs.tags }}
  #         labels: ${{ steps.meta.outputs.labels }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  # SSH 서버 배포 (Railway 사용 시 불필요)
  # Railway는 GitHub과 직접 연결되어 자동 배포됩니다.
  # deploy:
  #   name: 서버 배포
  #   runs-on: ubuntu-latest
  #   needs: [test, build-and-push]

  #   steps:
  #     - name: 서버 배포
  #       uses: appleboy/ssh-action@v1.0.3
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USER }}
  #         key: ${{ secrets.SERVER_SSH_KEY }}
  #         port: ${{ secrets.SERVER_PORT }}
  #         script: |
  #           # Docker 컨테이너 중지 및 제거
  #           docker stop study-with-backend || true
  #           docker rm study-with-backend || true

  #           # 새 이미지 pull
  #           docker pull YOUR_DOCKERHUB_USERNAME/study-with-backend:latest

  #           # 새 컨테이너 실행
  #           docker run -d \
  #             --name study-with-backend \
  #             --restart unless-stopped \
  #             -p 3000:3000 \
  #             -e DATABASE_URL="${{ secrets.DATABASE_URL }}" \
  #             -e ACCESS_TOKEN_SECRET="${{ secrets.ACCESS_TOKEN_SECRET }}" \
  #             -e REFRESH_TOKEN_SECRET="${{ secrets.REFRESH_TOKEN_SECRET }}" \
  #             YOUR_DOCKERHUB_USERNAME/study-with-backend:latest

  #           # 헬스 체크
  #           sleep 10
  #           curl -f http://localhost:3000/health || exit 1
